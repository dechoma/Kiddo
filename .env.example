# Configuration File Path
# Path to connectors.yaml file (defaults to connectors.yaml in project root)
CONNECTORS_CONFIG_PATH=connectors.yaml

# Gmail Connector Test Configuration
TEST_GMAIL_CREDENTIALS_PATH=creds/gmail_credentials.json
TEST_GMAIL_TOKEN_PATH=creds/test_gmail_token.json
# TEST_GMAIL_QUERY=is:unread -label:kiddo/test_processed newer_than:3d  # Optional, default will be used if not set
# TEST_GMAIL_PROCESSED_LABEL=kiddo/test_processed  # Optional, default: "kiddo/test_processed"

# LLM Configuration
# LLM_API_KEY=your-llm-api-key
# LLM_MODEL=gpt-4
# LLM_PROVIDER=openai
# LLM_PROMPTS_DIR=prompts

# Google Calendar Configuration
# GOOGLE_CALENDAR_CREDENTIALS_PATH=creds/calendar_credentials.json

# iCal Configuration
# ICAL_URL=https://calendar.example.com/ical

# SMS Configuration
# SMS_API_KEY=your-sms-api-key
# SMS_PROVIDER=twilio

# Email Configuration
# EMAIL_SMTP_SERVER=smtp.example.com
# EMAIL_SMTP_PORT=587
# EMAIL_USERNAME=your-email@example.com
# EMAIL_PASSWORD=your-password
# EMAIL_USE_TLS=true
